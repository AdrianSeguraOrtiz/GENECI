[project]
name = "GENECI"
version = "5.0.0"
description = "Software package whose main functionality consists of an evolutionary algorithm to determine the optimal ensemble of machine learning techniques for genetic network inference based on the confidence levels and topological characteristics of its results."
authors = [{ name = "adrian_segura", email = "adrianseor.99@uma.es" }]
license = "MIT"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "typer[all]>=0.23.1,<0.24.0",
    "docker>=7.1.0,<8.0.0",
    "pandas>=2.3.0,<3.0.0",
    "plotly>=6.5.2,<7.0.0",
    "iteround>=1.0.4,<2.0.0",
    "scipy>=1.15.3,<2.0.0",
    "matplotlib>=3.10.8,<4.0.0",
    "tqdm>=4.67.1,<5.0.0",
    "tornado>=6.5.4,<7.0.0",
    "scikit-learn>=1.7.0,<2.0.0",
]

[project.scripts]
geneci = "geneci.__main__:main"

[tool.poetry.group.dev.dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
